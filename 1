<!DOCTYPE html>
<html lang="zh-Hant">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>心理測驗</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
      max-width: 600px;
      margin: auto;
    }

    /* 進度條容器 */
    .progress-container {
      width: 100%;
      background: #eee;
      border-radius: 20px;
      height: 16px;
      margin-bottom: 20px;
      overflow: hidden;
    }

    /* 進度條本體 */
    .progress-bar {
      height: 100%;
      width: 0%;
      background: #7aa6ff;
      transition: width 0.3s;
    }

    /* 讓字體在不同裝置都保持清晰 */
    h2 {
      font-size: clamp(20px, 4vw, 28px);
    }
    label {
      font-size: clamp(16px, 3.5vw, 20px);
    }
    button {
      font-size: clamp(16px, 3.5vw, 20px);
      padding: 10px 16px;
      margin: 10px 5px;
      cursor: pointer;
      border-radius: 10px;
      border: none;
      background: #5b8bff;
      color: white;
    }
    button:disabled {
      background: #ccc;
      cursor: not-allowed;
    }

    #result {
      margin-top: 20px;
      padding: 20px;
      border-radius: 10px;
      background: #f4f4f4;
      font-size: clamp(16px, 3vw, 18px);
    }

  </style>
</head>
<body>
  <h2>心理測驗</h2>

  <!-- 進度條 -->
  <div class="progress-container">
    <div class="progress-bar" id="progressBar"></div>
  </div>

  <div id="quiz"></div>
  <button id="prevBtn" onclick="prevQuestion()" disabled>上一題</button>
  <button id="nextBtn" onclick="nextQuestion()">下一題</button>
  <div id="result"></div>

  <script>
    const questions = [
      {
        q: "1. 當你遇到一個新的挑戰/任務時，你的第一反應是：",
        options: ["(A) 馬上跳進去、先試看看", "(B) 先觀察環境、研究方式", "(C) 有點猶豫、怕搞砸、先做部分準備", "(D) 想幫助他人、在背後支撐或配合"]
      },
      {
        q: "2. 當朋友情緒低落時，你通常會：",
        options: ["(A) 拉著他們去做事情", "(B) 靜靜陪伴、傾聽", "(C) 有點退縮，不確定怎麼幫", "(D) 主動照顧或支持他們"]
      },
      {
        q: "3. 當你在思考人生方向時，你比較會：",
        options: ["(A) 設定目標立刻行動", "(B) 深入分析、寫筆記", "(C) 小心翼翼慢慢前進", "(D) 和別人一起交流、互相支持"]
      },
      {
        q: "4. 面對失敗，你最可能的反應是：",
        options: ["(A) 立刻再戰一次", "(B) 自我反省", "(C) 感到退縮、怕再失敗", "(D) 尋求人際支持"]
      },
      {
        q: "5. 你最看重自己的哪個特質？",
        options: ["(A) 行動力", "(B) 深度思考", "(C) 謹慎", "(D) 溫暖與支持"]
      }
    ];

    const results = {
      A: "你是【馬型】：你習慣扛責任、給人安全感，但別忘記你也需要被照顧。",
      B: "你是【狐狸型】：敏銳細心但收得太緊，試著多給世界一些機會。",
      C: "你是【男孩型】：敏感真誠、對自己疑惑很多，但你已經很好。",
      D: "你是【鼴鼠型】：溫柔體貼、讓人放鬆，但也要記得照顧自己。"
    };

    let current = 0;
    let answers = [];

    function updateProgressBar() {
      const percent = ((current) / questions.length) * 100;
      document.getElementById("progressBar").style.width = percent + "%";
    }

    function renderQuestion() {
      updateProgressBar();

      const q = questions[current];
      let html = `<p>${q.q}</p>`;
      q.options.forEach((opt, i) => {
        html += `
          <label>
            <input type="radio" name="q${current}" value="${opt[1]}" 
            ${answers[current] === opt[1] ? "checked" : ""}> ${opt}
          </label><br>
        `;
      });

      document.getElementById("quiz").innerHTML = html;

      document.getElementById("prevBtn").disabled = current === 0;
      document.getElementById("nextBtn").innerText = current === questions.length - 1 ? "提交" : "下一題";
    }

    function nextQuestion() {
      const selected = document.querySelector(`input[name=q${current}]:checked`);
      if (!selected) return alert("請先選擇一個選項！");

      answers[current] = selected.value;

      if (current < questions.length - 1) {
        current++;
        renderQuestion();
      } else {
        finishTest();
      }
    }

    function prevQuestion() {
      if (current > 0) {
        current--;
        renderQuestion();
      }
    }

    function finishTest() {
      const count = { A: 0, B: 0, C: 0, D: 0 };
      answers.forEach(a => count[a]++);

      let final = "A";
      for (let k in count) if (count[k] > count[final]) final = k;

      document.getElementById("quiz").innerHTML = "";
      document.getElementById("prevBtn").style.display = "none";
      document.getElementById("nextBtn").style.display = "none";

      document.getElementById("result").innerHTML = `<h3>${results[final]}</h3>`;

      document.getElementById("progressBar").style.width = "100%";
    }

    renderQuestion();
  </script>
</body>
</html>
